diff --git a/apps/dashboard/src/app/layout.tsx b/apps/dashboard/src/app/layout.tsx
index 0c05de8a..efd860a8 100644
--- a/apps/dashboard/src/app/layout.tsx
+++ b/apps/dashboard/src/app/layout.tsx
@@ -58,17 +58,11 @@ const BASE_METADATA: Metadata = {
   description: "Manage your deployments, domains, and infrastructure.",
   icons: {
     icon: [
-      { url: '/favicon.ico' },
-      { url: '/favicon-16x16.png', sizes: '16x16', type: 'image/png' },
-      { url: '/favicon-32x32.png', sizes: '32x32', type: 'image/png' },
+      { url: '/akyonlabs-favicon.svg', type: 'image/svg+xml' },
     ],
     apple: [
       { url: '/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
     ],
-    other: [
-      { url: '/android-chrome-192x192.png', sizes: '192x192', type: 'image/png' },
-      { url: '/android-chrome-512x512.png', sizes: '512x512', type: 'image/png' },
-    ],
   },
   manifest: '/site.webmanifest',
 };
diff --git a/apps/dashboard/src/components/logo.tsx b/apps/dashboard/src/components/logo.tsx
index 0c42b4e6..115c4264 100644
--- a/apps/dashboard/src/components/logo.tsx
+++ b/apps/dashboard/src/components/logo.tsx
@@ -1,13 +1,13 @@
-/**
- * Openship logo - a stylised "O" built with a bordered div.
- * Uses CSS border-foreground so it renders instantly (no JS theme check).
- */
 export function Logo({ size = 36, className }: { size?: number; className?: string }) {
   return (
-    <div
-      className={`shrink-0 rounded-full border-[3px] border-foreground ${className ?? ""}`}
+    <svg
+      viewBox="0 0 300 210"
+      fill="currentColor"
+      className={`shrink-0 ${className ?? ""}`}
       style={{ width: size, height: size }}
       aria-hidden="true"
-    />
+    >
+      <path d="M8 198 103 12h44L49 198Z M158 12h8v186h-38v-38H83l19-38h26V71Z M184 12h39v186h-39Z M235 161h51v37h-51Z"/>
+    </svg>
   );
 }
diff --git a/apps/dashboard/src/lib/product-view.ts b/apps/dashboard/src/lib/product-view.ts
index c87c0310..1db775a0 100644
--- a/apps/dashboard/src/lib/product-view.ts
+++ b/apps/dashboard/src/lib/product-view.ts
@@ -44,8 +44,8 @@ export function isProductView(value: unknown): value is ProductView {
  * pure function doesn't exempt it. Moving this back next to `useBrandName` throws
  * "Attempted to call brandNameFor() from the server" on every page load.
  */
-export function brandNameFor(brand: string, view: ProductView): string {
-  return view === "mail" ? `${brand} Mail` : brand;
+export function brandNameFor(_brand: string, view: ProductView): string {
+  return view === "mail" ? "Akyon Labs Mail" : "Akyon Labs";
 }
 
 export type ResolveProductViewInput = {
diff --git a/apps/dashboard/public/akyonlabs-favicon.svg b/apps/dashboard/public/akyonlabs-favicon.svg
new file mode 100644
index 00000000..547c97e0
--- /dev/null
+++ b/apps/dashboard/public/akyonlabs-favicon.svg
@@ -0,0 +1 @@
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64"><rect width="64" height="64" rx="14" fill="#101113"/><g transform="translate(6 13) scale(.18)" fill="#f4f4f5"><path d="M8 198 103 12h44L49 198Z M158 12h8v186h-38v-38H83l19-38h26V71Z M184 12h39v186h-39Z M235 161h51v37h-51Z"/></g></svg>
diff --git a/apps/dashboard/src/app/(dashboard)/layout.tsx b/apps/dashboard/src/app/(dashboard)/layout.tsx
index 75468ebd..ada0c30e 100644
--- a/apps/dashboard/src/app/(dashboard)/layout.tsx
+++ b/apps/dashboard/src/app/(dashboard)/layout.tsx
@@ -135,10 +135,6 @@ export default async function DashboardLayout({ children }: { children: React.Re
     );
   }

-  const initialGithubData = await serverApi
-    .get("github/home", { cache: "no-store" })
-    .catch(() => null);
-
   // Resolve the rail HERE, on the server, so the first painted sidebar is already
   // the right one. Doing it client-side from document.cookie would render the
   // platform rail and then flip the entire nav after hydration. Passing the
@@ -147,7 +143,6 @@ export default async function DashboardLayout({ children }: { children: React.Re

   return (
     <DashboardProviders
-      initialGithubData={initialGithubData}
       initialUser={session.user}
       selfHosted={deploymentInfo.selfHosted}
       deployMode={deploymentInfo.deployMode}
